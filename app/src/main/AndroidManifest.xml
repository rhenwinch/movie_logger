<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xcape.movie_logger">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movie_logger"
        tools:targetApi="31">
        <service
            android:name=".service.NotificationService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <activity
            android:name=".presentation.main_activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.auth.AuthPromptActivity"
            android:parentActivityName=".presentation.main_activity.MainActivity" />
        <activity
            android:name="com.xcape.movie_logger.presentation.login.LoginActivity"
            android:parentActivityName=".presentation.auth.AuthPromptActivity" />
        <activity
            android:name="com.xcape.movie_logger.presentation.sign_up.SignUpActivity"
            android:parentActivityName=".presentation.auth.AuthPromptActivity" />
        <activity
            android:name=".presentation.trending.TrendingActivity"
            android:parentActivityName=".presentation.main_activity.MainActivity" />
        <activity
            android:name=".presentation.watchlist.WatchlistActivity"
            android:parentActivityName=".presentation.main_activity.MainActivity" />
        <activity
            android:name=".presentation.search.SearchActivity"
            android:parentActivityName=".presentation.main_activity.MainActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".presentation.movie_details.MovieActivity"
            android:configChanges="orientation|screenSize|layoutDirection"
            android:parentActivityName=".presentation.trending.TrendingActivity" />
    </application>

</manifest>